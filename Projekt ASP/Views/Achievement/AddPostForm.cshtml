@model Projekt_ASP.Models.Post
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<form asp-action="AddPost" asp-controller="Achievement" method="post">
    <input type="hidden" asp-for="@Model.AchievementId" value="@ViewBag.AchievemntId" />
    <div>
        <div><span asp-validation-for="Title"></span></div>
        <label asp-for="Title">Tytuł postu</label>
        <input type="text" class="form-text" asp-for="Title" />
    </div>
    <div>
        <label asp-for="Text">Treść postu</label>
        <input type="text" class="form-text" asp-for="Text" />
    </div>
    <button type="submit">Dodaj Komentarz</button>
</form>